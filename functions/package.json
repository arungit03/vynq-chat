{
  "name": "vynq-chat-functions",
  "description": "Vynq-chat Cloud Functions — server-side auto-deletion, friend acceptance, notifications.",
  "type": "module",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "20"
  },
  "dependencies": {
    "firebase-admin": "^12.3.1",
    "firebase-functions": "^4.9.0"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  },
  "private": true
}
