# Supabase browser configuration. The publishable/anon key is safe in the
# browser only when every exposed table and Storage bucket uses RLS policies.
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=replace-me
